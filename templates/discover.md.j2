# Discover: {{ query }}
{% if notes | length > 0 -%}
{% for note in notes -%}
> {{ note }}
{% endfor %}

{% endif -%}
## Concepts
{% for group in groups -%}
### {{ group.label }}
{% for concept in group.concepts -%}
{% if ambiguous %}{{ loop.index }}.{% else %}-{% endif %} **{{ concept.label }}**{% if concept.primary_id %} (`{{ concept.primary_id }}`){% endif %}
{% if concept.synonyms | length > 0 -%}
Synonyms: {{ concept.synonyms | join(", ") }}
{% endif -%}
{% if concept.xrefs | length > 0 -%}
IDs: {{ concept.xrefs | join(", ") }}
{% endif -%}
{% if concept.sources | length > 0 -%}
Sources: {{ concept.sources | join(", ") }}
{% endif -%}

{% endfor -%}
{% endfor -%}
{% if plain_language -%}
## Plain Language
**{{ plain_language.title }}** (MedlinePlus)
{{ plain_language.summary_excerpt }}
{{ plain_language.url }}

{% endif -%}
## Suggested Commands
{% for command in next_commands -%}
- `{{ command }}`
{% endfor -%}
