# Articles: {{ query }}

{% if count == 0 -%}
No articles found matching the filters.

Try broader search terms or check spelling.
{% else -%}
Found {{ count }} article{% if count != 1 %}s{% endif %}

Semantic Scholar: {% if semantic_scholar_enabled %}enabled{% else %}disabled{% endif %}
{% if sort == "relevance" %}
Ranking: {{ ranking_policy }}
{% endif %}

| PMID | Title | Source(s) | Date | Why | Cit. |
|---|---|---|---|---|---|
{% for article in rows -%}
|{{ article.pmid }}|{% if article.is_retracted %}[RETRACTED] {% endif %}{{ article.title | truncate(60) }}|{{ article.sources }}|{{ article.date | default("-") }}|{{ article.why }}|{{ article.citation_count | default("-") }}|
{% endfor %}

Use `get article <pmid>` for details.
Filters: [query], -k/-q <keyword>, -g <gene>, -d <disease>, --drug <name>, -a <author>, --since <YYYY-MM-DD>, --type <research-article|research|review|case-reports|meta-analysis>, --open-access, --exclude-retracted, --sort <date|citations|relevance>, --source <all|pubtator|europepmc>
{% if pagination_footer %}

{{ pagination_footer }}
{% endif %}
{% endif %}
